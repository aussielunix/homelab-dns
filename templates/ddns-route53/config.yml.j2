credentials:
  access_key_id: "{{ ddns_route53_access_key_id }}"
  secret_access_key: "{{ ddns_route53_secret_access_key }}"

route53:
  hosted_zone_id: "{{ ddns_route53_zone_id }}"
  records_set:
    - name: "home.valueline.io"
      type: "A"
      ttl: 300
